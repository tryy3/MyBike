{
  "name": "mybike",
  "private": true,
  "workspaces": [
    "server",
    "client",
    "shared",
    "logging",
    "strava-webhook-proxy"
  ],
  "scripts": {
    "build:shared": "npm run -w shared build",
    "build:logging": "npm run -w logging build",
    "build:packages": "npm run -w shared build && npm run -w logging build",
    "test": "vp run -r test",
    "verify": "npm run build:packages && vp check && npm test",
    "prepare": "vp config"
  },
  "dependencies": {
    "drizzle-orm": "1.0.0-rc.4-5d5b77c"
  },
  "devDependencies": {
    "vite-plus": "0.2.4"
  },
  "overrides": {
    "drizzle-orm": "1.0.0-rc.4-5d5b77c",
    "vite": "npm:@voidzero-dev/vite-plus-core@0.2.4",
    "vitest": "4.1.10"
  },
  "devEngines": {
    "packageManager": {
      "name": "npm",
      "version": "11.17.0",
      "onFail": "download"
    }
  },
  "engines": {
    "node": ">=26.0.0"
  },
  "allowScripts": {
    "esbuild@0.18.20": true,
    "esbuild@0.25.12": true,
    "esbuild@0.28.1": true
  }
}
